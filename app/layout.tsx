import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Softvic - Victor Opawumi | Web & Mobile App Developer',
  description: 'Portfolio of Victor Opawumi, Frontend Engineer specializing in React.js, Laravel, Kotlin, and modern web technologies. Based in Lagos, Nigeria.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}

